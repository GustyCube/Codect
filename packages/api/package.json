{
  "name": "@codect/api",
  "version": "1.0.0",
  "description": "Codect API server",
  "scripts": {
    "dev": "nodemon --exec 'python' main.py",
    "start": "python main.py",
    "test": "python3 -c \"import sys, unittest; suite = unittest.defaultTestLoader.discover('.', pattern='test_*.py'); result = unittest.TextTestRunner().run(suite); sys.exit(0 if result.wasSuccessful() else 1)\"",
    "lint": "ruff check ."
  },
  "dependencies": {
    "@codect/core": "^1.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
